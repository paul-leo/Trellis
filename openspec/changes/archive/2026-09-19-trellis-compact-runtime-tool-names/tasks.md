# Tasks

- [x] 1.1 Rename the Gateway native entry to `trellis` and add ownership-gated
      migration from `trellis-gateway`.
- [x] 1.2 Compact built-in Runtime tool presentation names while preserving
      original provider routing and metadata.
- [x] 1.3 Update the built-in Runtime Skill and related docs to use discovery
      rather than hardcoded MCP prefixes.
- [x] 2.1 Add unit tests for compact names, original-name dispatch, legacy
      Gateway-key migration, and metadata.
- [x] 2.2 Build, test, install locally, sync Claude/Pi, and verify Claude's
      effective MCP names and real Runtime startup.
