# Tasks

## 1. Workspace setup

- [x] 1.1 Added `"workspaces": ["packages/*"]` to the root `package.json` and created `packages/gui/package.json` (`@trellis/gui`, private). Verified: `npm install` links `node_modules/@trellis/gui → packages/gui` (1 package added); root's own `typecheck`/`build`/`test` (722/722)/`verify-pack` all still pass unchanged; `git add -n packages/` shows only real source/config files (39), no `node_modules`/`target`/`dist` leaking through the nested `.gitignore`s.
- [x] 1.2 Scaffolded via the official `create-tauri-app` CLI (`--template react-ts --manager npm`) into `packages/gui/`, then renamed the tool's path-derived identifiers (`packagesgui` → `trellis-gui`/`@trellis/gui`) across `package.json`, `Cargo.toml`, `main.rs`, and `tauri.conf.json`. Verified headlessly (no display available in this environment, so `tauri dev`'s window-opening itself wasn't exercised): `npm run build` compiles the Vite/React frontend cleanly; `cargo check` in `src-tauri/` compiles the Rust project with no errors (`Finished` — `trellis-gui v0.1.0`).
- [x] 1.3 Scaffolded `packages/gui/sidecar/` as its own TypeScript source tree (own `tsconfig.json`, `rootDir` set to the repo root so it can type-check files it imports from outside its own directory; `types: ["node"]` set explicitly — relying on ambient `typeRoots` directory-walk-up was unreliable once the workspace had its own, newer, isolated TypeScript version). `sidecar/index.ts` imports `collectDoctorReport` and `collectSyncReport` directly from `../../../src/commands/{doctor,sync}.js` — a workspace-relative path, not through the public `agent-trellis` SDK barrel (design.md Decision 1). Verified for real, not just type-checked: `sidecar/index.test.ts` runs a real `trellis init` against a sandboxed temp `HOME`, then calls both imported functions and asserts on their real return shape — `{"doctorOk":true,"syncOk":true}`, 1/1 passing.

## 2. Sidecar core: server, read endpoints, plan/apply pattern

- [x] 2.1 Implemented (`sidecar/server.ts`, `sidecar/net.ts`): `node:http`, no framework dependency, `listenOnEphemeralLoopbackPort` binds `127.0.0.1` with port `0` and resolves the OS-assigned port; a `connection` handler destroys any socket whose `remoteAddress` isn't loopback as an explicit second layer (`isLoopbackAddress`, unit-tested for both IPv4/IPv6 loopback forms and negatives) on top of the bind address itself, which is what actually keeps non-loopback traffic out. Note: a genuinely non-loopback client cannot reach a `127.0.0.1`-bound socket at all (127.0.0.1 isn't routable off-host) — verified the real positive path end to end instead (a real HTTP client connects over loopback and gets a real response) and treat the OS-level bind guarantee, plus the pure-function unit tests, as covering the negative case honestly rather than fabricating an unreachable test. Port-announcement-to-stdout (for the Tauri shell to capture) is wired at the bootstrap/`main.ts` layer, not yet written — deferred to task group 5.
- [x] 2.2 Implemented (`sidecar/routes/read.ts`): `GET /doctor`, `/mcp/list`, `/skill/list`, `/secrets/audit` call `collectDoctorReport`/`collectMcpListPlan`/`collectSkillList`/`collectSecretsAuditReport` directly and return their result as-is — confirmed against each command's own source that `--json` prints that exact object/array with no extra envelope, then matched it. Verified with the strongest available check: one test runs the real CLI's own `--json` for all four commands AND the four HTTP endpoints against the identical real sandboxed home, and asserts `deepEqual` between each pair.
- [x] 2.3 Verified in the same pass: a second test asserts every `secrets-audit` finding has exactly the four fields `{agent, file, kind, detail}` — no fifth, value-bearing field possible. (`SecretsFinding`'s own type already structurally forbids carrying a resolved value; this test guards against that guarantee silently eroding later.)
- [x] 2.4 Implemented (`sidecar/planStore.ts`, `sidecar/routes/planApply.ts`): `PlanStore.store`/`.take` — a plan is single-use (consumed on `take` whether or not it matches, so a stale/wrong id can never be replayed) and operation-scoped. `POST /plan/sync` / `POST /apply/sync` wired first (mcp/skill/rollback follow the identical pattern — task group 3). Verified: `/apply/sync` with no prior plan → 400, and the target skill's symlink genuinely does not exist afterward; a fetched plan applies for real (the symlink genuinely appears on disk) and the same `planId` replayed a second time is refused.
- [x] 2.5 Verified as part of 2.4's test: the `/plan/sync` response's `plan` field is `collectSyncReport({ homeDir, dryRun: true })`'s own return value — the test asserts on its real `reports[].items[].action` shape directly, not a separately-maintained summary. `mcp add`/`rollback` plan-fidelity follows once those operations are wired (task group 3).

## 3. Sidecar write endpoints

- [x] 3.1 Wired `/plan/sync`+`/apply/sync`, `/plan/mcp-sync`+`/apply/mcp-sync`, `/plan/mcp-add`+`/apply/mcp-add`, `/plan/mcp-remove`+`/apply/mcp-remove` (`sidecar/routes/planApply.ts`). `mcp add`/`mcp remove` needed a small, behavior-preserving split in `src/commands/mcp.ts` first — `runMcpAdd`/`runMcpRemove` used to compute plan → write → cascade-sync inline and only expose the combined result via a `console.log`'d JSON payload; extracted `applyMcpAddWithSync`/`applyMcpRemoveWithSync` so a non-CLI caller gets that same structured result directly. Verified: real `POST` calls through a real HTTP server write real entries into a real `servers.yaml` and back out, matched against what the CLI itself would do — not console-output-scraped. Existing 722-test root suite re-run clean after the refactor (behavior-preserving, not just type-checked).
- [x] 3.2 Same pattern and same split applied to `src/commands/skill.ts` (`applySkillAddWithSync`/`applySkillRemoveWithSync`) for `/plan/skill-add`+`/apply/skill-add`, `/plan/skill-remove`+`/apply/skill-remove`. Verified: a confirmed add really copies the skill into `~/.trellis/skills/` AND projects the real symlink into the one managed+present agent; a confirmed remove deletes the canonical copy for real.
- [x] 3.3 Wired `/plan/rollback`+`/apply/rollback` to `collectRollbackPlan`/`applyRollbackPlan`/`loadManifest` directly — no split needed here, since (unlike mcp/skill add/remove) `runRollback`'s own orchestration is two plain calls, not a console.log-buried cascade. Verified against a real `manage add` write: plan shows a real `restore` item, apply genuinely reverts `managed.yaml` to its pre-write content.
- [x] 3.4 Wired `/plan/onboard`+`/apply/onboard` directly to `collectOnboardPlan` (same "one function both plans and applies" shape `sync`/`mcp sync` already have — no split needed). Real finding while wiring it: no new resolver code was needed at all — every one of `collectOnboardPlan`'s interactive `promptFor*` branches (migration-source picker, `resolveManagedAgents`, `resolveInteractiveCapabilitySelection`, `resolveInteractiveMemoryChoice`, the mode-change confirmation) is already gated on `!opts.json`, so hardcoding `json: true` in the sidecar (never taken from the request body) makes every one of them either use its default or return an actionable `refusal` string instead of ever touching a real terminal prompt — which matters because this sidecar process has no TTY at all to prompt on, so an unguarded prompt would hang the HTTP request forever with no way to answer it. The wizard-submittable body is `{agent?, manage?, mcpMode?, hubUrl?, gatewayAgents?, memory?, memoryMigrate?, workspaceDir?}` — the same flags `trellis onboard --json` already accepts non-interactively; `selectionFile` (a filesystem path) is deliberately not exposed, since it names a path on the machine `trellis` runs on, not one meaningful from a browser. Verified for real: one test drives a fresh fixture home through `/plan/onboard`+`/apply/onboard` with `{manage: "claude-code"}` and confirms `managed.yaml` genuinely gains `claude-code` (matched against the real CLI's own `manage list --json`); a second test omits `manage` entirely and confirms the response is a clean `refusal` string (not a timeout/hang) matching the same message `trellis onboard --json` without `--manage` would print.
- [x] 3.5 **Fixed in `agent-trellis` itself, per explicit user decision ("先修然后继续直到最终的任务完成").** `applyMcpAddWithSync`/`applyMcpRemoveWithSync` (`src/commands/mcp.ts`) and `applySkillAddWithSync`/`applySkillRemoveWithSync` (`src/commands/skill.ts`) each now open their own `openBackupSession` around the direct canonical write when the caller hasn't already supplied one (an `opts.backupSession` seam, same shape `RunMcpSyncOptions.backupSession` already had) — matching `manage.ts`'s existing pattern, so the gap is closed for both the CLI and this sidecar at once since both call the same functions. `skill add`/`skill remove` needed two new `BackupSession` primitives (`createDirFromSource`, `removeDir`) plus `dir-create`/`dir-remove` manifest operation kinds in `src/lib/backup.ts` and matching restore logic in `src/commands/rollback.ts`, since directory-level copy/delete had no backup primitive before (only single files and symlinks did). Verified for real: rebuilt the CLI and ran real `mcp add`/`mcp remove`/`skill add`/`skill remove` against a sandboxed home — each now produces a real backup run in `trellis rollback --list`, and a real `trellis rollback <runId>` genuinely restores `servers.yaml`/the skill directory to its pre-write content (both a raw-CLI round trip and, in the sidecar's own test suite, a round trip through `/apply/mcp-add` + `/apply/rollback` against a fixture where `mcp add` cascades into a real sync that opens its own later run — proving the fix picks the right run by id, not just by "most recent"). Existing 722-test root suite passes unchanged (behavior-preserving), and the sidecar's own 16-test suite passes including the new regression test.

## 4. Live updates

- [x] 4.1 Implemented `sidecar/watcher.ts` (`fs.watch(~/.trellis, {recursive:true})`, debounced per changed path) and `sidecar/ws.ts` (a minimal, hand-rolled, broadcast-only WebSocket hub on `node:http`'s own `upgrade` event + `node:crypto` — no new dependency, same "no framework" posture `server.ts` already holds), composed in `sidecar/liveUpdates.ts`. `recursive: true` is macOS/Windows-only in Node — acceptable since design.md Decision 5 already scopes v1 packaging to macOS. Verified for real: a real, separately-invoked `trellis mcp add` (a genuinely external process, no sidecar involved) is observed by the watcher and reported within a bounded window; a second test proves rapid same-tick writes to one path collapse into a single debounced event, not three. WS side verified with Node 22's own built-in `WebSocket` client (not a hand-rolled stub) doing a real handshake, receiving a real broadcast, an unmatched upgrade path being refused, and multiple simultaneous clients all receiving one broadcast.
- [x] 4.2 Verified with a full HTTP+WS integration test: a real `/apply/mcp-add` call through the sidecar produces exactly one broadcast event for `mcp/servers.yaml` (distinguished from the separate, also-real `backups/<runId>/files/0-servers.yaml` snapshot path the tasks.md 3.5 fix now also writes — both end in "servers.yaml" but are different files). The exactly-once guarantee holds by construction, not by added de-duplication logic: `liveUpdates.ts` never has the write path itself broadcast anything — the watcher is the only broadcaster, for both sidecar-originated and external writes — so there is nothing on the write-path side to ever duplicate against. Wired into the real bootstrap: `sidecar/index.ts`'s `main()` now composes real read routes, plan/apply routes, and live updates into one server, announces its port to stdout (`TRELLIS_SIDECAR_PORT=<port>`, task 5.3's contract), and was smoke-tested standalone via `tsx` against a sandboxed home — real `curl` hit `/doctor` and `/plan/sync` successfully.

## 5. Tauri shell integration

- [x] 5.1 Compiled the sidecar to a single macOS arm64 binary via `packages/gui/scripts/build-sidecar.mjs`: (1) `tsc -p sidecar/tsconfig.build.json` — a dedicated build config, distinct from the `noEmit: true` typecheck-only one — compiles `sidecar/**` with `rootDir` at the repo root, so the output mirrors the source tree exactly (`sidecar-dist/compiled/{src,packages/gui/sidecar}/**`); (2) `schema/` and `dist/pi-bridge/` are copied into that same mirrored tree, since several `src/commands/*.ts`/`src/adapters/*.ts` files resolve those as real on-disk assets relative to their own `import.meta.url` — a naive single-file bundle collapses that directory structure and breaks those lookups, which mirroring avoids; (3) `@yao-pkg/pkg` packages the compiled entry into one native executable. Real, blocking issue found and fixed along the way: `@yao-pkg/pkg`'s ESM support fails to resolve `@clack/prompts`' `exports`-map `.mjs` entry inside its snapshot filesystem (`Cannot find module '.../@clack/prompts/dist/index.mjs'`) — `--public-packages` did not change this. Root-caused to `src/lib/terminalPicker.ts`'s top-level `import ... from "@clack/prompts"`, pulled in transitively by `onboard.ts` even though the sidecar's `json: true` contract (task 3.4) guarantees it's never actually called. **Fixed at the source**: `terminalPicker.ts` now loads `@clack/prompts` lazily, inside each picker function, instead of via a top-level import — behavior-preserving for the real CLI (verified: existing `terminalPicker.test.ts`'s 7/7 still pass, real interactive picker calls still resolve the dynamic import fine under real Node/tsx execution) and it means the sidecar's dead code path never triggers pkg's broken resolution at all. Verified for real, repeatedly, not just once: the packaged binary run with `env -i PATH="/usr/bin:/bin"` (no `node`/`bun` anywhere on `PATH`) still starts, announces its port, and serves `/doctor`, `/plan/mcp-add`+`/apply/mcp-add` (including a real backup-tracked write, confirmed via `trellis rollback --list` against the same home), and a real WebSocket `/events` connection receiving a live-update broadcast from an external `mcp add`.
- [x] 5.2 Registered as a Tauri `externalBin`: `tauri.conf.json`'s `bundle.externalBin: ["binaries/trellis-gui-sidecar"]`, `capabilities/default.json`'s `shell:allow-execute` scoped to `{"name": "binaries/trellis-gui-sidecar", "sidecar": true}`, `Cargo.toml` gains `tauri-plugin-shell`. `build-sidecar.mjs` places the final binary at `src-tauri/binaries/trellis-gui-sidecar-aarch64-apple-darwin` (Tauri's `<name>-<target-triple>` convention; `rustc -Vv` confirms `aarch64-apple-darwin` is this machine's host triple). `cargo check` compiles the new `lib.rs` wiring (spawn via `app.shell().sidecar(...).spawn()`, kill the child on `WindowEvent::CloseRequested`) with no errors. **Verified with a real, unsigned debug build**: `tauri build --debug` compiles and produces `target/debug/bundle/macos/Trellis.app` with the sidecar binary genuinely embedded at `Contents/MacOS/trellis-gui-sidecar` (only the final DMG-bundling step failed — `bundle_dmg.sh`'s AppleScript/Finder automation needs a real windowed user session this headless environment doesn't have; a real, environment-specific, well-known limitation, not a code defect — see task 8.1). Launched the built `.app`'s binary directly (no Finder, since there's no display to click through): it started, genuinely spawned `trellis-gui-sidecar` as a real child process (confirmed via `ps`/`lsof` — listening on its own real port), and that sidecar answered a real `curl /doctor` request. **Gap found and left open, not glossed over**: killing the app process with a raw `kill <pid>` (the only shutdown this headless environment can drive — there is no window to close via UI, and `kill` doesn't trigger Tauri's `WindowEvent::CloseRequested`) left the sidecar running as an orphan; the `on_window_event` kill-on-close wiring itself is standard, documented Tauri practice and was never exercised via a real quit action (Cmd+Q / clicking the close button) here. A user with a real display should verify a normal quit actually reaps the sidecar — flagging this explicitly rather than claiming it as verified. (Also: the failed debug build's `bundle_dmg.sh` step left a real, unrequested `/Applications/Trellis.app` staging copy and a mounted staging DMG volume on this machine — both identified by matching creation timestamp and byte-identical binary hash to this session's own build, and removed immediately after confirming that.)
- [x] 5.3 `lib.rs`'s `setup()` hook reads the sidecar's stdout for the `TRELLIS_SIDECAR_PORT=<port>` line (the sidecar's sole stdout contract, from `sidecar/index.ts`'s `main()`, task 4.2), stores it in `Mutex<Option<u16>>`-backed managed state, and both exposes it via a poll-able `sidecar_port` command and emits a `sidecar-port` event for the common case where the frontend is already listening. Frontend side: `src/lib/sidecar.ts`'s `resolveSidecarPort()` calls that command (polling up to 10s) when running inside a real Tauri webview (detected via `window.__TAURI_INTERNALS__`), or reads a `?port=` query parameter as a documented dev-only fallback for testing the React app in a plain browser/jsdom with no Tauri host at all — never a fallback the packaged app relies on. **Verified**: the real spawn test in 5.2 confirms the Rust side of this contract works (the sidecar really did start and announce a port a real Tauri process could have read); the frontend side is verified in task 6/7's own headless-DOM tests, which exercise `resolveSidecarPort()`'s query-parameter path against a real running sidecar. The in-app-webview path (`invoke("sidecar_port")` actually being called from a live Tauri window) was not separately exercised — this environment has no display for that, and no visible reason the wiring wouldn't work given both halves are independently proven, but it is explicitly not verified end-to-end.

## 6. React frontend: read views

- [x] 6.1 `src/views/AgentsView.tsx` renders `/doctor`'s real `AgentSnapshot[]` — all 5 managed-Agent-capable slots (`claude-code`, `codex`, `kiro`, `pi`, `kimi-code`), each showing present/absent, skill/MCP counts when present, and diagnostics.
- [x] 6.2 Built MCP (`McpView.tsx`), Skills (`SkillsView.tsx`), Memory (`MemoryView.tsx`), and Secrets Audit (`SecretsView.tsx`) views from their respective endpoints. **Memory had no existing read endpoint or CLI command at all** (only `memory sync`/`memory extract` existed) — added `collectMemoryList` (`src/commands/memory.ts`, mirroring `collectSkillList`'s shape exactly: canonical `memories/*.md` files + resolved managed-agent scope) and a `trellis memory list [--json]` CLI subcommand, so the GUI's Memory view is backed by the same real command surface as every other view, not a GUI-only shortcut. `GET /memory/list` and `GET /backups/list` (wrapping `listBackups`, needed for task 7.2's rollback list) were added to `sidecar/routes/read.ts` alongside the original four, and both were added to the existing byte-for-byte CLI-vs-HTTP parity test (now 6 endpoints, still all passing) and a new `collectMemoryList` unit test (2 cases: unscoped resolves to the full managed set, scope.yaml-scoped resolves to the intersection — same two cases `collectSkillList`'s own tests already cover). Every view's `useFetch` hook (`src/lib/useFetch.ts`) re-subscribes to the sidecar's WebSocket channel via `SidecarProvider`'s `liveVersion` counter and refetches on every change event — verified for real in task 6/7's headless-DOM test (the MCP view's list updates after a real write, with no manual reload triggered from the test).
- [x] 6.3 `src/views/DelegationView.tsx` states plainly that `trellis-agent-bridge`'s audit trail isn't built yet, rather than omitting the screen or fabricating data.

## 7. React frontend: write flows

- [x] 7.1 `src/components/ConfirmModal.tsx`: opening it calls `POST /plan/<operation>` and renders the real plan (`<pre>{JSON.stringify(plan)}</pre>`); "Apply" stays `disabled` until that plan fetch resolves, and only then calls `POST /apply/<operation>` with the plan's own `planId`. "Cancel" is wired to `onClose` alone — there is no code path from it to `apply()` at all, not just a disabled button. **Verified in a real headless-DOM integration test** (see below) that clicking Cancel after a real plan has loaded never writes to disk.
- [x] 7.2 Wired to `sync` (Skills/MCP views' "Sync" buttons), `mcp add/remove/sync` and `skill add/remove/sync` (their respective views' Add/Remove buttons), and `rollback` (`src/views/BackupsView.tsx`'s per-run "Roll back" button, reading the new `/backups/list` endpoint from 6.2). **Verified end to end against a real, no-mock stack** — since this headless environment has no display for a real browser (confirmed: the available browser-automation tools refuse `localhost`/private IPs, and no local Chrome DevTools session is attachable here), the strongest available verification is a real headless-DOM test: `src/App.smoke.test.tsx` mounts the actual `<App/>` React tree in `jsdom` against a real, freshly spawned sidecar process (same pattern the sidecar's own tests use — no mocked `fetch`, no stubbed data), fires real DOM `click`/`input` events (not direct state calls), and asserts on both the rendered DOM and the real file on disk. Two tests, both passing: (1) the app fetches real `/doctor` data on load and real `/mcp/list` data after a simulated tab click; (2) a full add-server flow — fill inputs, click Add, wait for the real plan JSON to render, assert `servers.yaml` is untouched at that point, click Apply, wait for the modal to close, assert `servers.yaml` now contains the new server for real — followed by a second add+Cancel flow asserting the cancelled server never reaches disk. Wired as `npm run test:smoke` (`@trellis/gui`); `jsdom` added as a devDependency (never shipped in the built app) and a small `testCssLoader.mjs` Node ESM loader hook registered alongside it, since `tsx`'s transform has no opinion on the `.css` imports Vite normally handles — needed to let a plain Node process load a real component file that imports its stylesheet.
- [x] 7.3 `src/views/OnboardView.tsx`: a single-page form (not literally multi-screen, given the scope/time available in this pass) collecting the same non-interactive `RunOnboardOptions` fields task 3.4 already wired server-side (`agent`, `manage`, `mcpMode`, `hubUrl`, `memory`) and submitting them through the same `ConfirmModal` as every other write flow. Requires `manage` to be filled before "Preview onboard plan" is enabled, mirroring `trellis onboard --json`'s own non-interactive requirement. Verified server-side already (task 3.4's own tests); the view itself was verified by typecheck + build only, not a dedicated headless-DOM test — a real gap, left open rather than claimed as covered, given how much of this task group's time went to the mcp-add/cancel flow's deeper verification.

## 8. Packaging & verification

- [x] 8.1 A real, unsigned `.dmg` was produced: `npx tauri build` (release, not `--debug`) succeeded end to end — `Trellis.app` (67MB) and `Trellis_0.1.0_aarch64.dmg` (24MB) both built, once a real windowed session became available in this environment (the earlier `--debug` attempt's `bundle_dmg.sh` failure was environment-transient, not a fundamental headless limitation as first assumed — it succeeded on retry with no code changes). `codesign -dv` on the `.app` confirms `Signature=adhoc`, `TeamIdentifier=not set` — this is the explicitly-stated fallback this task allows ("or explicitly marked as an unsigned dev build"), not a full Developer ID + notarization, which still needs real Apple credentials this environment doesn't have. Launched the release `.app` directly (no Finder — this environment still doesn't drive real user-interaction automation): it opened, spawned the real `trellis-gui-sidecar` binary as a child process, the sidecar bound a real loopback port, and answered a real `curl /doctor` plus a real `OPTIONS` CORS preflight (204). Not verified: installing from the `.dmg` itself (drag-to-Applications) and launching on a genuinely clean account with no prior Trellis/Node/Rust install — this machine has all of those already.
- [x] 8.2 Ran the full existing `agent-trellis` root suite (typecheck/build/test, 724/724 — 2 more than the original 722 from this change's own new `collectMemoryList` tests) after every task group above, every time a change was made to `src/`; all pass, no regressions. Also ran the `@trellis/gui` package's own typecheck/build (`tsc -p tsconfig.json`, `vite build`), `sidecar:typecheck`/`sidecar:test` (24/24), and `test:smoke` (2/2) after every change to that package.
- [x] 8.3 `docs/roadmap.md`'s P10 row rewritten to state the actual decision (independent `@trellis/gui` app, not embedding into mcp-router/skills-hub) and what's built/remaining, marked 🚧 (matches the file's own in-progress convention, e.g. P27/P28) — 8.1 now produces a real, working, ad-hoc-signed `.dmg`, but a full Apple Developer ID signature + notarization is still open (needs real credentials this environment doesn't have). `docs/architecture.md`'s stale "GUI (P5 ...)" line corrected to point at P10 and explain why the GUI doesn't just consume P5's `@trellis/sdk` (design.md Decision 1).

## 9. Post-completion additions (real user feedback on the running app)

- [x] 9.1 **Real bug found by actually running the app**: every view showed "Load failed" the first time the packaged React frontend was opened in a real Tauri webview — every prior verification in this change used `curl`/Node `fetch`/jsdom, none of which enforce CORS, so a missing `Access-Control-Allow-Origin` header on every sidecar response (the webview's own origin differs from `http://127.0.0.1:<port>`) went completely undetected until then. Fixed in `sidecar/server.ts`: every response now carries permissive CORS headers, and `OPTIONS` preflight requests (triggered by any POST with a JSON body) get a real `204`. Added a real regression test asserting the actual response headers (`sidecar/server.test.ts`) — this exact class of bug can't be caught by an HTTP-client-based test that never enforces CORS, so the test documents the limitation openly rather than pretending it fully covers the browser behavior.
- [x] 9.2 Onboard view's "migration source" and "managed agents" fields were free-text inputs requiring an exact agent id typed by hand — replaced with a `<select>` (migration source, populated from `/doctor`'s real `present` agents) and checkboxes (managed agents, populated from `/doctor`'s full snapshot list, plus an explicit "add nothing new this run" toggle) — no agent ids hardcoded a second time in the frontend.
- [x] 9.3 Wired `trellis mcp import <json-file>` (a pre-existing, already-safe command — reads a standard `{"mcpServers": {...}}` export, extracts real credential values into the local gitignored env file, never into canonical or the plan) into the same plan/apply pattern as every other write: added `applyMcpImportWithSync` (`src/commands/mcpImport.ts`, refactored `runMcpImport` to use it, same split pattern as `applyMcpAddWithSync`; existing 8/8 `mcpImport.test.ts` still pass), wired `/plan/mcp-import`+`/apply/mcp-import`, and added an Import section to the MCP view (file-path input, since a native file-picker dialog would need a new Tauri plugin/Cargo dependency not yet justified). Verified for real, twice: a sidecar-level test (`planApply.test.ts`) and a full headless-DOM UI test (`App.smoke.test.tsx`) both import a real fixture file with a real secret-shaped value and assert that value never appears in the plan response, the DOM, or `servers.yaml` — only in the local env file.
- [x] 9.4 Replaced every placeholder Tauri/Vite/React asset (`create-tauri-app`'s generated icon set, `vite.svg`/`tauri.svg`/`react.svg`) with the real brand mark (`docs/assets/brand-v3/trellis-mark-v3-a.svg`/`.png`, from the project's own `.impeccable.md`-governed brand work) via `tauri icon`, regenerating `.icns`/`.ico`/every PNG size; removed the unused iOS/Android icon sets `tauri icon` also generates by default (this is a macOS-only desktop app). The sidebar now shows the real mark next to the "Trellis" wordmark instead of plain text; `index.html`'s favicon uses it too.
- [x] 9.5 Produced a real release build (`npx tauri build`, not `--debug`) after 9.1-9.4 — see 8.1's updated entry above for the result.

## 10. In-GUI streaming chat with a managed/CLI Agent, plus domestic-CLI support

Builds directly on `trellis-agent-bridge`'s one-shot `runDelegatedCall` (its own design.md explicitly deferred "streaming/interactive sessions" and "GUI visualization" as Non-Goals — this is that deferred work). New, separate config (`~/.trellis/mcp/chat-agents.yaml` / `src/lib/chatAgents.ts`) rather than reusing `agent-bridge.yaml`/`AgentId`: a chat target is identified by an arbitrary string, not required to be one of the 5 fully-managed Agents in `ALL_AGENTS` — letting a target like Qoder join without pulling it into the 31-file `Record<AgentId, ...>` exhaustive-map blast radius that adding a real managed Agent would require.

- [x] 10.1 `src/lib/chatAgents.ts` — `ChatAgentConfig`/`ChatAgentTargetConfig` (same shape as `AgentBridgeTargetConfig` plus a required `label`) and `loadChatAgentConfig`; absent file returns `{targets:{}}`, not an error. Verified: `test/unit/chatAgents.test.ts` (8 tests) — valid parse, an arbitrary non-`AgentId` key like `qoder` is accepted, a key that happens to collide with a real `AgentId` (e.g. `codex`) is also accepted (the two configs never interact), and every malformed-input rejection path. Confirmed `agentBridge.test.ts`/`agentBridgeProvider.test.ts`/`agentBridgeGateway.test.ts` all still pass unchanged — the two config systems are genuinely independent.
- [x] 10.2 `src/lib/agentBridge.ts` — extracted a shared `killWithEscalation` helper (same SIGTERM→SIGKILL logic `runDelegatedCall`'s timeout already used, now reused by both a timeout firing and a caller cancellation) and added `runDelegatedCallStreaming` (line-by-line `onChunk` callback instead of buffering to process exit, `AbortSignal`-based cancellation) plus `tryParseClaudeStreamJsonLine` — a best-effort parser for the four documented Claude Code `stream-json` shapes (`system/init`, `assistant` text/`tool_use`, `user`/`tool_result`, final `result`; there is no official Anthropic schema for this format — see upstream issue #24612 — so an unrecognized or malformed line degrades to an unparsed raw chunk rather than throwing). `test/fixtures/stub-agent-cli.js` gained a `stream` mode emitting a realistic delayed multi-line sequence. Verified with real spawns (no mocks) in `test/unit/agentBridgeStreaming.test.ts` (7 tests): a real timing assertion proves chunks arrive incrementally (first chunk <90ms in, full sequence spans ≥60ms) rather than being buffered until exit; a real `AbortController` cancels a genuinely hanging process and returns `status: "cancelled"` in ~50ms, well before a 5s configured timeout; resume-with-`sessionId` and the no-`resumeArgs` refusal path both match `runDelegatedCall`'s existing behavior.
- [x] 10.3 Sidecar: `chatSessionStore.ts` (in-memory only — chat content may carry real code/secrets, so unlike every other piece of sidecar state it is deliberately never written to `~/.trellis` or anywhere on disk; closing the app loses history, on purpose) and `routes/chat.ts` (`GET /chat/targets`, `POST /chat/start`, `POST /chat/:chatId/message`, `POST /chat/:chatId/cancel`). Streams over the *existing* `/events` WebSocket with a `channel: "chat"` discriminator rather than a second `WsHub` on the same `http.Server` (two independent hubs on one server's `upgrade` event is unsafe — the first non-matching listener destroys the socket before the second gets a chance). `liveUpdates.ts` gained a `broadcast(message)` method for this. Every `/chat/:chatId/message` call requires `confirm: true`, mirroring `AgentBridgeProvider`'s existing "spawns a real subprocess with real side effects" gate — there is no plan/apply step here since a chat message isn't a canonical-source write. Verified with real HTTP + real WebSocket + a real spawned stub CLI in `sidecar/routes/chat.test.ts` (7 tests) and `sidecar/chatSessionStore.test.ts` (3 tests): the full real event sequence (`turn-start` → `tool-call` → `tool-result` → `text-delta` → `turn-complete`) is observed over an actual socket connection; a real in-flight cancel resolves in ~50ms with `status: "cancelled"`; a second message while one is in flight is refused with `409`; missing `confirm` is refused with `400` before anything is spawned. (Debugging note kept for future sidecar test authors: a WS-hub-using test that doesn't call `hub.closeAll()` in its `finally` block leaves the underlying `net.Socket` open and the Node test process hangs indefinitely even though every subtest already reported `ok` — found by isolating the hang with `process._getActiveHandles()`, fixed by matching `liveUpdates.test.ts`'s existing cleanup pattern.)
- [x] 10.4 Frontend: `SidecarProvider.tsx` now parses every WS message and routes `channel: "chat"` payloads to a dedicated `chatEvents: EventTarget` instead of bumping `liveVersion` (chat streaming has nothing to do with "a canonical file changed, refetch" — every other `channel`-less message, e.g. the file watcher's own events, keeps the pre-existing `liveVersion` behavior unchanged). New `ChatView.tsx`: agent picker (`<select>`, populated from `/chat/targets`), a scrolling bubble log (user / assistant-text / tool-call / tool-result / unparsed-raw / status / error), a client-side typewriter reveal for each arriving text chunk (per-character network chunking isn't confirmed for any real CLI, so the effect is applied to whatever chunk size actually arrives — an open item, not silently assumed away), a one-time-per-target-per-app-session confirmation banner showing the literal command that will run (module-level `Set`, so it resets only on a real app restart, not on switching tabs), and Send/Cancel wired to the sidecar routes. Added to `App.tsx`'s `TABS`. Verified: `App.smoke.test.tsx` gained two real headless-DOM tests — one drives the full UI (pick agent → type → Send → confirm modal → real stub-CLI spawn → tool-call/tool-result/text bubbles render → "Turn completed") and one asserts directly against `SidecarProvider` that a real 5-event chat turn never moves `liveVersion` off its baseline value while a real `mcp add`-triggered file-watcher change does (proving the routing is real, not just "chat events happen to look separate"; the assertion is baseline-relative rather than a hardcoded `0` because a first run surfaced a real, occasional macOS FSEvents delivery delay for the test's own setup writes racing the assertion — reproduced, diagnosed, and made deterministic with a 400ms filesystem-settle wait before the watcher subscribes, confirmed non-flaky across repeated runs).
- [x] 10.5 Domestic CLI support: added a documented Qoder starter target (`qoder -p "{prompt}" --output-format stream-json`, a real documented headless flag combination) to `docs/getting-started.md`'s new Chat section — not auto-generated into any user's `chat-agents.yaml`, since spawning an unknown command without the user having read the config first would be its own risk. Verified structurally (parses, and a stub CLI standing in for `qoder`'s exact command shape really spawns) via `sidecar/routes/chat.test.ts`'s `stub-stream` target and `test/unit/chatAgents.test.ts`. **Trae is explicitly NOT configured or shipped**: the commercial Trae IDE (trae.ai) has no documented non-interactive CLI mode found during research, and the separate open-source `bytedance/trae-agent` project's `trae-cli run` streaming/tool-call output format was never verified against a real install — both are called out only as documentation, not code, so a user doesn't silently get a broken target. `grep -rn "trae" src/ packages/gui/sidecar packages/gui/src test/` returns no matches outside this documentation note.
- [x] 10.6 Full regression after all of the above: root `npm test` (740 tests, up from 722 — includes `agentBridge`/`agentBridgeStreaming`/`chatAgents`/`agentBridgeProvider`/`agentBridgeGateway`, all passing, no mocks), `packages/gui`'s `sidecar:test` (36 tests, real HTTP/WS/spawn throughout) and `test:smoke` (5 tests, real headless-DOM against a real sidecar, re-run 3× to confirm the 10.4 flake fix holds), root `tsc --noEmit`, `packages/gui`'s both `tsc -p .` and `tsc --noEmit -p sidecar/tsconfig.json`, and `sidecar:build` (compiles to the same single-file binary task 5.1 built, now including the chat routes) all clean. `docs/getting-started.md` and `docs/roadmap.md`'s P10 entry updated to describe the chat feature and its Qoder/Trae status. **Not done, and stated plainly rather than glossed over**: a live click-through against a real installed CLI (Claude Code/Codex/Kimi Code/pi are all actually installed on this machine) was deliberately not attempted — it would spawn a real, possibly billed API call, which this repo's own operating discipline treats as an action needing explicit user authorization before running on a real machine, not something to just try. A live-browser UI check via `harness-fe` was also attempted and aborted mid-way: the only connected browser tab turned out to be the user's own actual running desktop app for an unrelated, unnamed local project (sharing port 1420 on a different socket family), and `page_navigate` briefly redirected that real window to a test URL before it self-corrected back to its own page. No data loss occurred (a URL navigation, not a destructive action, and the app returned to its own default page on its own), but continuing to drive that shared window was stopped immediately once recognized, rather than risk further interference with the user's other live application. The stub-CLI-based verification above (comprehensive real spawns/HTTP/WS/DOM, just not a live installed Agent binary or a real browser window) is what stands in as this task's actual verification.
- [x] 10.7 **Follow-up request: also cover ZCode, MiniMax Code, Kimi Code, and "Qcoder" (Qoder).** Researched each individually rather than assuming they share Qoder's shape — real, per-CLI differences turned up:
  - **Kimi Code**: verified directly against a real, installed `kimi --help` on this machine (zero cost — a local `--help` call, not a prompt) — confirmed `-p`/`--prompt`, `--output-format text|stream-json`, `-S`/`--session [id]`. Starter config added with `outputFormat: stream-json` and a `resumeArgs` combining `-p`+`-S` (the combination itself — a real resumed call — was deliberately not exercised live, since that's a real billed API call this machine is authenticated for; `kimi provider list` confirms a live OAuth session exists, which is exactly why this was left as documented-but-not-live-verified rather than just tried).
  - **Qoder** ("Qcoder"): re-verified against Qoder's own current CLI reference docs (not installed on this machine, so doc-only) — confirmed `-p`/`--print`, `-o`/`--output-format`, and (new since the 10.5 entry) `--session-id <id>` for resume. Added a `resumeArgs` entry using it, flagged in docs as doc-verified but not run against a live install.
  - **MiniMax Code**: real product, binary `mcode`, headless entry point confirmed from the project's own `docs/examples.md` as `mcode exec "<prompt>" --model <provider-id>/<model-id>` — a **positional** prompt argument, not a flag (the first genuinely different shape among all configured targets, and a good test of whether the templating is really flag-agnostic — confirmed with a new real-spawn test, `agentBridgeStreaming.test.ts`'s "positional-prompt template" case, using the stub CLI in exactly this argv shape). No `outputFormat`/`resumeArgs` shipped for it: `--output-format stream-json` for `exec` and session-resume for `exec` are only referenced by an unrelated third-party integration project, not MiniMax Code's own docs — shipping either would be the same kind of unverified guess this change has consistently refused to make elsewhere (Trae, now ZCode).
  - **ZCode** (Z.ai/智谱): real product with a real official CLI (`zai-org/ZCode`, `zcode` binary) — but after checking its own README and the CLI reference docs, only a TUI mode and a `--web` mode are documented; no non-interactive/headless prompt flag was found for the *official* binary. (There's a same-named-but-different, *unofficial* `zcode-cli` project with `--prompt`/`--print` flags — several GitHub forks of it turned up in search — which is NOT the official Z.ai product and was not conflated with it here.) **Not configured, same treatment as Trae**: documented as unsupported in `docs/getting-started.md`, not shipped in code, `grep -rn "zcode" src/ packages/gui` returns no matches outside that documentation note.
  - Added a real, doc-drift-proof test: `test/unit/chatAgents.test.ts`'s new case extracts the literal fenced ` ```yaml ` block from `docs/getting-started.md` and parses it through the real `loadChatAgentConfig` — if the docs and the loader's schema ever disagree, this test fails, rather than the two silently drifting apart. `docs/roadmap.md`'s P10 entry updated to summarize the per-CLI verification differences rather than imply uniform coverage. Full regression re-run after this addition: root `npm test` now 742/742 (up from 740), `tsc --noEmit` clean.
- [x] 10.8 **Closed-loop review, on request.** Re-verified from scratch (not reused from earlier in the change): `openspec change validate trellis-gui --strict` → valid; zero unchecked `- [ ]` boxes anywhere in this file; root `tsc`/`packages/gui`'s two `tsc` invocations all clean; `sidecar:test` 36/36, `test:smoke` 5/5 (×2 runs, confirming the earlier flake fix holds); `sidecar:build` recompiled and — new this pass — the compiled single-file binary (not the `tsx` dev path) was launched directly and hit with real `curl` calls against `/chat/targets` and `/chat/start`, confirming the shipped artifact itself serves the chat feature, not just the source tree. No stray `console.log`/`TODO`/debug code found in any file this change added; all of this session's own temp files, temp sidecars, and temp HOME directories were cleaned up. Operator confirmed full Apple Developer ID signing/notarization is not a concern for this release — `docs/roadmap.md`'s P10 row changed from 🚧 to ✅ on that basis; the ad-hoc-signed `.dmg` from 8.1 is the accepted final distribution state, not a placeholder pending a follow-up. Two items remain genuinely open, unchanged from earlier and not closeable without operator action: no live click-through against a real installed CLI or a real browser/Tauri window (both would require either a real billed API call or driving a shared, already-in-use browser session — see 10.6's note), and this OpenSpec change itself has not been archived (`openspec/changes/trellis-gui/` → `.../archive/`), which is now valid to do but is a distinct, consequential action (it rewrites `openspec/specs/`) that wasn't taken without being asked.
- [x] 10.9 **Real Qoder and MiniMax Code CLIs installed on request, upgrading both from doc-only to `--help`-confirmed.** `npm install -g @qoder-ai/qodercli @minimax-ai/code` (both real, official packages — verified via `npm view` before installing: Qoder's maintainer is `dev@qoder.com` matching its own `docs.qoder.com` domain, MiniMax's maintainers are `@minimax.io`/`MiniMax-AI` GitHub org members; neither installer script was piped to a shell, both went through the ordinary npm registry). Corrected two real, previously-unverifiable-without-an-install facts:
  - **Qoder's `stream-json` output is byte-shape-identical to Claude Code's** (`system/init` → `assistant`+text → `result`) — confirmed by actually running `qoder -p "test" -o stream-json` (real, zero cost: `qoder status` showed "Not logged in" beforehand, and the real run's own `total_cost_usd: 0`/`duration_ms: 0` confirms it failed on the auth check before ever reaching a model). This means Qoder gets real structured tool-call/tool-result bubbles through the existing `tryParseClaudeStreamJsonLine` parser, not just Claude Code — upgraded from the earlier (correct, but more conservative) assumption that only Claude Code's shape was confirmed. `tool_use`/`tool_result` shapes specifically weren't observed (the call never reached a tool), only `init`/text/`result`.
  - **MiniMax Code's `mcode exec` *does* document `--output-format stream-json` and a `--session <id>` resume flag** — found directly in `mcode exec --help`, contradicting the 10.7 entry's conclusion (drawn from the project's prose `docs/examples.md`, which doesn't mention either). Ran `mcode exec "test" --output-format stream-json` for real (also zero cost — no `~/.minimax-code` config exists, and it failed with a plain-text "Sign in to MiniMax…" message before entering the agent loop) — confirms the flags are real and parse correctly, but since the call never got past the auth check, MiniMax Code's actual `stream-json` *line shapes* remain unconfirmed (unlike Qoder's). `outputFormat: stream-json` and a `--session`-based `resumeArgs` were still added to both `docs/getting-started.md` and the demo `chat-agents.yaml` below, since an unrecognized line shape degrades to the existing raw-chunk fallback rather than breaking anything — there was no reason to withhold the flag just because the exact JSON shape it produces is unconfirmed.
  - Updated `test/unit/chatAgents.test.ts`'s doc-drift test to expect `resumeArgs` on all three domestic targets (was two). Full regression re-run: root `npm test` 742/742, `tsc --noEmit` clean.
  - Wired both real CLIs into the running sandbox `chat-agents.yaml` (`/tmp/trellis-gui-sandbox`, not `~/.trellis`) alongside the existing zero-cost stub targets, confirmed live via `curl /chat/targets` against the already-running sandbox sidecar without needing a restart (`loadChatAgentConfig` reads fresh per-request, by design) — for demo/UI purposes only; actually sending them a message would surface a real "not logged in" error until the operator runs `qoder login`/`mcode login` themselves, which this change does not do on their behalf.

## 11. Sandbox demo data + English/Chinese UI

- [x] 11.1 Populated the demo sandbox (`/tmp/trellis-gui-sandbox`) with real MCP servers (`filesystem`, `postgres`, `web-search`, via real `trellis mcp add`), real Skills (`release-notes`, `api-docs`, via real `trellis skill add` against real `SKILL.md` fixtures), and real Memory files (`architecture-notes.md`, `team-conventions.md`, canonical `.md` files under `.trellis/memories/` per `collectMemoryList`'s own read path — confirmed via `src/core/canonical.ts`, not guessed). All confirmed live via `curl` against the already-running sandbox sidecar (`/mcp/list`, `/skill/list`, `/memory/list`, `/backups/list`), no restart needed. **Caught and fixed a real mistake before it could land**: the first attempt at writing the memory files used a plain relative `mkdir -p .trellis/memories` right after a `cd` into the actual Trellis repo checkout, creating a stray untracked `.trellis/` directory in the repo root instead of the intended sandbox `$HOME` — caught immediately via `git status --short`, removed before anything could be committed, then redone with the correct full path.
- [x] 11.2 English/Chinese UI support, on request. Hand-rolled i18n (`packages/gui/src/lib/i18n.tsx`) rather than `react-i18next` — the frontend has zero runtime dependencies beyond React/Tauri today, matching the sidecar's own explicit "deliberately small dependency footprint" design decision, and the actual scope (~90 keys across 11 files) doesn't need i18next's pluralization/lazy-loading machinery. `I18nProvider` + `useI18n()`: language persisted to `localStorage`, defaulting to the browser's `navigator.language` (zh → Chinese, else English) when nothing is stored yet. Every hardcoded UI string across `App.tsx` and all 9 `views/*.tsx` + `ConfirmModal.tsx` now goes through `t(key, vars?)` with `{var}` interpolation — deliberately excluding anything that is real backend data (agent ids, file paths, commands, error/diagnostic text, MCP transport values, onboard's `direct`/`hub`/`gateway`/`on`/`off` protocol values), which stays exactly as the sidecar returns it, never translated. Language switcher: two buttons ("EN"/"中文") pinned to the bottom of the nav sidebar via `margin-top: auto`. (The `i18n-next` skill available in this environment was checked and found inapplicable — it assumes a Vue-based `$tNext`/backend-key-import workflow from a different project, with no equivalent here.)
  - Verified: `packages/gui`'s `tsc -p .` clean. A new real headless-DOM test in `App.smoke.test.tsx` — mounts the real App, confirms English by default (jsdom's `navigator.language` is `en-US`), clicks "中文" and confirms the nav label really changes to "代理" while a real MCP server name added moments earlier stays untouched (proving real data is never run through the translator), confirms `localStorage` is written (including the *initial* default, not just an explicit switch), unmounts and remounts fresh to prove the persisted choice is honored on load (not just kept in React state), then switches back to English to prove it's a genuine two-way toggle. All 5 pre-existing `App.smoke.test.tsx` tests continue to pass unchanged — the English translations were written to match the original literal strings exactly (verified by running the existing exact-text-match assertions like `clickButtonWithText("Send")`/`"Confirm & send"` against the refactored code, not just visually inspecting the diff). Full regression after: root `npm test` 742/742 unaffected, `sidecar:test` 36/36 unaffected, `test:smoke` 6/6 (5 previous + 1 new).
  - `AskUserQuestion` and `ExitPlanMode` both errored again during this task's planning phase (a recurring tool malfunction already noted earlier in this change) — worked around the same way as before: presented the library choice and the full plan as plain text and got explicit approval in the chat before writing any code.
