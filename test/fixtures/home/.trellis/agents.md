# Fixture canonical instructions

Used only by Trellis's own sandboxed tests (trellis-sync-p1). Symlinked
out to each agent's own instructions file location by `trellis sync
instructions` — never loaded by a real agent outside the test sandbox.
